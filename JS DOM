1.document.getElementById、document.getElementsByTagName、document.getElementsByClassName
  这三种方法得到的都是对象。后两种是数组对象，所以要访问的话，形式为[i]。

2.选择器 querySelector()、querySelectorAll()
  
  ** 注意：（）中为CSS选择器，可以是（"#id名"）、（".class名"）等等等等
     若不存在匹配的元素，则这两种类型调用该方法时，均返回null。

  ** querySelector():
     返回匹配的第一个对象
      
  ** querySelectorAll():
     返回匹配的全部对象
     
    
