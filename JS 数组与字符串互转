一、数组转字符串
1.toString()方法：返回数组的字符串表示，每个值的中间以逗号分隔，调用每一项的toString()方法
2.toLocalString()方法：返回数组的字符串表示，每个值的中间以逗号分隔，调用每一项的toLocalString()方法
3.join()方法：可以使用不同的分隔符开构建字符串，eg：arr.join('|') --表示数组中每一项以|分隔连成字符串

二、字符串转数组
1.split()方法
